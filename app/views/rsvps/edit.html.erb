<section id="rsvp-update" class="container content-section text-center">
  <div class="row">
    <div class="col-lg-8 col-lg-offset-2">
      <h2 class="regular-case">Please RSVP here for Tom and Sal's wedding</h2>
      <br>

      <% unless @rsvp.blank? %>
        <%= nested_form_for @rsvp, url: {action: :update}, html: {class: "form"} do |f| %>
          <%= f.hidden_field :id %>
          <p>Please let us know if you can't wait to celebrate with us or if you're going to be sad and miss all the fun. Click the name to toggle the status.</p>
          <div class="css3-checkboxes text-left .col-xs-6 .col-sm-4 centered">
            <%= f.fields_for :invitee, :wrapper => false do |i_f| %>
              <%= i_f.hidden_field :id %>
              <label class="large-text"><%= i_f.check_box :coming, {} %>
              <span><%= i_f.label :coming, i_f.object.name %></span></label><br>
            <% end %>
          </div>
          <br>
          <p>If you have any special dietary requirements or wish to make any other comments leave them below and click the button at the bottom to send us your rsvp.<br><%= f.text_area :comments, size: "40x5", class: "txtarea-glow" %></p>
          <br>
          <%= f.submit class: "btn btn-default btn-lg" %>
        <% end %>

      <% else %>
        <p>Whoops, it seems that you mistyped your password. Check your invitation again just to be sure.</p>
        <%= form_tag("/rsvp", method: "get") do %>
          <%= text_field_tag :q, "" , class: "txt-glow", placeholder: "Code" %>
          <%= submit_tag "Enter", class: "btn btn-invert" %>
        <% end %>
        <br>
        <p>Alternatively you can call or SMS Sal on 0422 257 630.</p>
      <% end %>
      <br><br>
    </div>
  </div>
</section>
